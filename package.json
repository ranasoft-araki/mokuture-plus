{
  "name": "mokuture",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && uv sync"
  }
}
